[package]
name = "remote_control"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
eframe = "0.29"
egui = "0.29"
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }
scrap = "0.5"

[target.'cfg(windows)'.dependencies]
winreg = "0.52"
